---
sidebar_position: 1
title: API keys and scopes
---

# API keys and scopes

{/* TODO: full scope catalog */}

- Creating API keys on the Account page
- How keys are authenticated (`Authorization: Bearer <key>` header) and stored (hashed)
- Keys work for both the REST API and the [MCP server](mcp); each MCP tool requires the scope matching what it does
- Scope catalog:

| Scope | Allows |
| --- | --- |
| teams:read / teams:write | ... |
| team_members:read / write | ... |
| team_projects:read / write | ... |
| users:read / users:write | ... |
| projects:read / projects:write | ... |
| work_items:read / work_items:write | ... |
| entries:read / entries:write | ... |
| relationships:read / relationships:write | ... |

- Revoking keys
